#ifndef IMGPROCESS_H
#define IMGPROCESS_H

#include "opencv.h"
/*

//using namespace cv;
using namespace std;

class ImgProcess
{
private:
cv::Mat inimg;
cv::Mat outimg;
cv::Mat face_clipping; // 切出的人脸的图片
CvRect face_box;
public:
ImgProcess(cv::Mat image):inimg(image),face_box(cvRect(0,0,50,20)){}
void clip_face(); // 裁剪人脸
void draw_face(); // 调用face_detector函数，并在人脸上画框
cv::Mat OutFace();
cv::Mat Outputimg();
};
*/

#endif // IMGPROCESS_H
